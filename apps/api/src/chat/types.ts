/**
 * Chat / Reviews / Notifications types, resolved from the generated
 * shared contract (ADR-0003) — no hand-maintained duplicates.
 */

export type {
  ConversationDto,
  MessageDto,
  ReviewSummaryDto,
  NotificationDto,
} from '@khabir/shared-types';
export type { SendMessageInput, CreateReviewInput } from '@khabir/shared-validation';
