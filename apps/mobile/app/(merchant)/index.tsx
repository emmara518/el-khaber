import { ShellPlaceholder } from '../../src/features/shell/shell-placeholder';

/** Merchant home — shell placeholder (Phase 1, no product logic). */
export default function MerchantHomeRoute() {
  return <ShellPlaceholder roleAr="تاجر" titleAr="الرئيسية" />;
}
