import MerchantSettingsScreen from '../../src/features/merchant/settings/merchant-settings-screen';

/**
 * Route entry: Merchant Settings (M-B) — ACCOUNT settings
 * (profile entry, verification entry, onboarding entry,
 * notifications placeholder, support, logout).
 */
export default function MerchantSettingsRoute() {
  return <MerchantSettingsScreen />;
}
