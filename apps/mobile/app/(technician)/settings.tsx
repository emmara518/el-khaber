import TechnicianSettingsScreen from '../../src/features/technician/settings/technician-settings-screen';

/**
 * Route entry: Technician Settings (T-E) — ACCOUNT SETTINGS
 * (profile edit entry, notifications entry, verification entry,
 * reviews entry, support, logout). The public profile remains a
 * separate screen.
 */
export default function TechnicianSettingsRoute() {
  return <TechnicianSettingsScreen />;
}
